# Through this yaml file we are creating sample pod in k8 cluster in specific namespace
---
 apiVersion: v1
 kind: Pod
 metadata:
    name: dev-pod
    namespace: dev
    labels:
        name: sample 
 spec:
    containers:
        - name: automation
          image: centos
          ports:
            - containerPort: 80
