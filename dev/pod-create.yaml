# Through this yaml file we are creating sample pod in k8 cluster in specific namespace
---
 apiVersion: v1
 kind: Pod
 metadata:
    name: dev_pod
    namespace: dev
    labels:
        name: dev 
 spec:
    containers:
        - name: automation
          image: centos
          ports:
            - containerPort: 80
